We handle idempotent requests using the Stripe API's idempotency keys. A retry mechanism with exponential backoff and jitter prevents transient failures from causing duplicate charges. Our webhook verification system uses HMAC-SHA256 signatures to protect against replay attacks. Set the Content-Type header to application/json for all API requests.