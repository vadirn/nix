## Bash (Claude Code)

The shell sandbox uses a permission allowlist. Single commands match allowlist entries; chained commands (`&&`, `||`, `;`) bypass them and trigger prompts. Prefer one command per call.

## Personal context

@~/Documents/vault/PERSONAL.md
