{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Vault project config",
  "description": "Per-repo config at .claude/.vault.config.json linking a project to its vault folder.",
  "type": "object",
  "required": ["vault_root", "project_path"],
  "additionalProperties": false,
  "properties": {
    "vault_root": {
      "type": "string",
      "pattern": "^/",
      "description": "Absolute path to the Obsidian vault root."
    },
    "project_path": {
      "type": "string",
      "pattern": "^/",
      "description": "Absolute path to the project folder inside the vault."
    }
  }
}
