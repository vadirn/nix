{
  "name": "distill-text",
  "version": "1.0.0",
  "private": true,
  "description": "Engine for the distill skill: extract a note's typed knowledge graph and project it as a span-anchored canonical note. Driven by the distill-text CLI on PATH; see SKILL.md.",
  "scripts": {
    "test": "bun test src",
    "fmt": "oxfmt src",
    "fmt:check": "oxfmt --check src",
    "lint": "oxlint src",
    "typecheck": "tsc --noEmit",
    "dead": "knip",
    "cycles": "madge --circular --extensions ts --ts-config tsconfig.json src"
  },
  "devDependencies": {
    "@types/bun": "1.3.4",
    "oxlint": "1.73.0",
    "typescript": "7.0.2"
  }
}
