---
type: note
description: "Impression distance in plein-air blocking."
epistemic_status: in-review
---

# Impression distance

## Abstract

Blocking from the impression rather than the scene keeps the painting's distances honest: the anchor image is fixed before any mixing starts, and every later judgment measures against it rather than the drifting scene.

## Concepts

### Anchor image

The first felt impression of the scene, fixed as the reference all later value judgments measure against.

### Impression distance

The nearness of a value to its anchor on re-inspection.

## Procedures

### Block from the impression

1. Fix the anchor image before opening paints
2. Re-check values against the anchor, not the scene

<!-- interact: pick-any id=residue -->

Residue triage. Checked `recover:` re-renders the entry from its fenced source (spliced verbatim if it fails the grade again); checked `keep:` keeps the entry exactly as it stands above; unchecked means the entry is removed from the final note.

- [ ] recover: `Impression distance` — inverted: def asserts nearness where source asserts a gap
  ```
  Impression distance is the gap between the felt sense of a scene and what
  the eye verifies on re-inspection; the painting should honor the former.
  ```

- [ ] recover: procedure:Block from the impression:2 — workflow: drying precondition missing from steps
  ```
  Before glazing, let the underlayer dry fully; a damp underlayer lifts and
  muddies the glaze.
  ```

- [ ] keep: `Anchor image` — gate-inconclusive: judge returned no verdict after retry
  ```
  The anchor image is the first felt impression, fixed before mixing begins.
  ```

<!-- /interact -->

<!-- interact: confirm-all id=triage-final dest=impression-distance.md src=sha256:3f9c2a1b8d4e -->

Check this last, on the device you will apply from.

- [ ] reviewed: residue triage above is final — apply writes impression-distance.md and deletes this file

<!-- /interact -->
