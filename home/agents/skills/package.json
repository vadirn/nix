{
  "name": "skills",
  "private": true,
  "workspaces": [
    "libs/*",
    "textkit",
    "imagen-fal/scripts",
    "vidgen-fal/scripts",
    "imagen-nanobanana/scripts"
  ],
  "scripts": {
    "check:bun": "bun run scripts/check-bun-version.ts",
    "typecheck": "bun run check:bun && bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "oxlint": "1.73.0",
    "typescript": "7.0.2"
  }
}
