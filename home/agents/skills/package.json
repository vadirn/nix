{
  "name": "skills",
  "private": true,
  "workspaces": [
    "_shared/*",
    "textkit",
    "*/scripts"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "@types/bun": "1.3.4",
    "oxlint": "1.73.0",
    "typescript": "7.0.2"
  }
}
